import { Component } from '@angular/core';

@Component({
  selector: 'score',
  templateUrl: './score.component.html',
  styleUrls: ['./score.component.css']
})
export class ScoreComponent {

  /*players: string[] = []; // Hier kann man die Spielernamen hinzufügen
  scores: number[] = []; // Hier kann man die Punktestände der Spieler hinzufügen

  addPlayer(playerName: string): void {
    if (playerName && !this.players.includes(playerName)) {
      this.players.push(playerName);
      this.scores.push(0);
    }
  }

  updateScore(playerIndex: number, scoreChange: number): void {
    if (playerIndex >= 0 && playerIndex < this.scores.length) {
      this.scores[playerIndex] += scoreChange;
    }
  }*/

}
